Lidar_front_end:
   lidar_type: 1   # 1 for Livox-avia, 3 for Ouster-OS1-64
   N_SCANS: 6
   using_raw_point: 1
   point_step: 1
   blind: 2.6
   
r3live_common:
   if_dump_log: 0                   # If recording ESIKF update log. [default = 0]
   record_offline_map: 1            # If recording offline map. [default = 1]
   pub_pt_minimum_views: 3          # Publish points which have been render up to "pub_pt_minimum_views" time. [default = 3]
   minimum_pts_size: 0.01           # The minimum distance for every two points in Global map (unit in meter). [default = 0.01] 
   image_downsample_ratio: 1        # The downsample ratio of the input image. [default = 1]
   estimate_i2c_extrinsic: 1        # If enable estimate the extrinsic between camera and IMU. [default = 1] 
   estimate_intrinsic: 1            # If enable estimate the online intrinsic calibration of the camera lens. [default = 1] 
   maximum_vio_tracked_pts: 600     # The maximum points for tracking. [default = 600]
   append_global_map_point_step: 4  # The point step of append point to global map. [default = 4]

r3live_vio:
   image_width: 672
   image_height: 376
   camera_intrinsic:
      [264.9425, 0.0, 334.3975,
      0.0,  264.79, 183.162,
      0.0, 0.0, 1.0 ] 
   camera_dist_coeffs: [-0.0442856, 0.0133574, 0.0, 0.0, 0.0]  #k1, k2, p1, p2, k3
   # Fine extrinsic value. form camera-LiDAR calibration.
   camera_ext_R:
         [0.9995897597632202, 0.017170804625958682, -0.02292325554984076,
            0.023146589349355498, -0.01292276982900426, 0.9996485569546115,
            0.016868538110887142, -0.9997690560766732, -0.013314913952324876]
   camera_ext_t: [-0.08518352335378968, 0.1257734831824673, -0.20411145029272001] 
   # Rough extrinsic value, form CAD model, is not correct enough, but can be online calibrated in our datasets.
   # camera_ext_R:
   #    [0, 0, 1,
   #     -1, 0, 0,
   #     0, -1, 0]
   # camera_ext_t: [0,0,0] 
   
r3live_lio:        
   lio_update_point_step: 4   # Point step used for LIO update.  
   max_iteration: 3           # Maximum times of LIO esikf.
   lidar_time_delay: 0        # The time-offset between LiDAR and IMU, provided by user. 
   filter_size_corner: 0.5
   filter_size_surf: 1.0
   filter_size_surf_z: 0.5
   filter_size_map: 0.5
   cube_side_length: 200 # 1000
   lidar_ext_R: [ 1,0,0,
                  0,1,0,
                  0,0,1]
   lidar_ext_t: [ 0, 0, 0.28] # lidar to imu